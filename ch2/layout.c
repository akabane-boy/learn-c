#include <stdio.h>

int main(void)
{
	/*
	printf("To C, or not to C:
			That is the question\n");
	*/
	/* This is wrong. We can't split them. */

	printf("To C, or not to C: That is the question\n");

	/* We can think of a C program as a series of 'tokens'. */

	return 0;
}
